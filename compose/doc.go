// Package compose provides generic types and helpers for constructing email template data
// It defines the configuration, recipient, and URL types that drive Go template variable
// substitution, along with helpers that assemble the data map passed to the template renderer
package compose
